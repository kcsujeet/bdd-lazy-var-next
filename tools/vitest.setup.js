// Vitest setup file for bdd-lazy-var-next
// Load the global dialect to use $varName syntax in tests
require('../global');

// Optional: Setup chai for assertions if not using vitest's built-in expect
// import { expect } from 'chai';
// import chaiSpies from 'chai-spies';
// chai.use(chaiSpies);
// global.expect = expect;
